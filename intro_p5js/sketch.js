function setup() {
  createCanvas(400, 400);
}
//when you are adding numbers the color of//the shape will change
//then adding the shape on background we can put//rect square acquired then changing numbers the shape will 
//change the positision

function draw() {
  background(220,0,100);
  rect(200,0,150);
}